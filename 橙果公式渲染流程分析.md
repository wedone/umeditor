# 橙果错题本公式渲染流程完整分析

## 📋 核心流程概述

橙果错题本使用的是**纯客户端渲染方案**，整个流程在浏览器端完成，**不依赖远程服务器**。

## 🔄 完整渲染流程（6 步）

### 1️⃣ 用户编辑公式
- 用户在 UMEditor 中使用公式按钮插入 LaTeX 公式
- 公式以 **MathQuill HTML 结构**保存在编辑器中
- 外层容器：`<span class="mathquill-embedded-latex">LaTeX代码</span>`

### 2️⃣ 点击保存按钮触发
```javascript
// action.js - getContent()
function getContent() {
    var cont = UM.getEditor("my-editor").getContent();
    saveEditedText(cont);
}
```

**关键代码（已注释，但逻辑清晰）：**
```javascript
// action.js 第 135-137 行（注释掉的代码揭示了真实逻辑）
// $('.tem-save-editor')[0].innerHTML = newText;
// var mathLatex = $('.tem-save-editor .mathquill-embedded-latex');
// mathFormulaToImg(mathLatex, sendUrl, param);
```

### 3️⃣ LaTeX → HTML（使用 KaTeX）
```javascript
// mathFormulaToImg.js - 第 28-37 行
for(var j = 0; j < mathFormula.length; j++){
    var latex = mathFormula[j].innerHTML;
    
    // 设置容器样式
    mathFormula[j].style.display = 'inline-block';
    mathFormula[j].style.paddingBottom = '3px';
    mathFormula[j].style.paddingTop = '2px';
    
    if(latex && !latex.includes('<img')){
        // ✨ 核心：使用 KaTeX 渲染 LaTeX → HTML
        katex.render(latex, mathFormula[j], {
            throwOnError: false
        });    
    }
}
```

### 4️⃣ SVG → Canvas（使用 canvg.js）
```javascript
// mathFormulaToImg.js - svgTocanvas()
function svgTocanvas(mathFormula){
    var svgElem = $(mathFormula).find('svg');
    for(var i = 0; i < svgElem.length; i++){
        var node = svgElem[i];
        var svg = node.outerHTML.trim();
        var canvas = document.createElement('canvas');
        
        // ✨ 核心：使用 canvg 库将 SVG 转为 Canvas
        canvg(canvas, svg);
        
        // 替换 SVG 为 Canvas
        parentNode.removeChild(node);
        parentNode.appendChild(canvas);
    }
}
```

**为什么需要这一步？**
- KaTeX 渲染的公式包含 SVG 元素（数学符号、分数线等）
- html2canvas **不能直接截取 SVG**（存在渲染问题）
- 必须先用 canvg 将 SVG 转为 Canvas，html2canvas 才能正确处理

### 5️⃣ HTML → Base64 图片（使用 html2canvas）
```javascript
// mathFormulaToImg.js - 第 42-51 行
var canvasWidth = mathFormula[j].offsetWidth; // ✨ 记录原始宽度
var canvasWidthObj = {};
canvasWidthObj[j] = canvasWidth;

svgTocanvas(mathFormula[j]); // 先转换 SVG

html2canvas(mathFormula[j], {
    fontSize: 8,
    fontWeight: 100,
    allowTaint: true,  
    taintTest: false,  
}).then(function(canvas){
    var url = canvas.toDataURL(); // ✨ 生成 Base64 图片
    domUrlArr.push({
        url: url,
        width: canvasWidthObj[index-1], // 保留原始宽度
    });
})
```

### 6️⃣ 替换为 `<img>` 标签并保存
```javascript
// mathFormulaToImg.js - 第 53-64 行
var timer = setInterval(function(){
    if(domUrlArr.length){
        for(var j=0; j < mathFormula.length; j++){
            var latex = mathFormula[j].innerHTML;
            if(latex.includes('katex-mathml')){
                // ✨ 核心：替换为 <img> 标签（Base64 内嵌）
                mathFormula[j].innerHTML = 
                    '<img class="cg-math-formula" width="' + 
                    domUrlArr[j].width + 'px" src="'+ 
                    domUrlArr[j].url +'" />';
                mathFormula[j].style.paddingBottom = '0';
            }
        }
        clearMathFormulaBox(); // 清理 MathQuill 容器
        saveEditTxt(sendUrl, param); // 保存到服务器
        clearInterval(timer);
    }
}, 1000)
```

### 7️⃣ 清理 MathQuill 容器
```javascript
// mathFormulaToImg.js - clearMathFormulaBox()
function clearMathFormulaBox() {
    var editAfterTxt = $('.tem-save-editor .mathquill-embedded-latex');
    
    // 将 <img> 提取出来
    for(var i = 0; i < editAfterTxt.length; i++){
        $(editAfterTxt[i]).before(editAfterTxt[i].innerHTML);
    }
    
    // 删除 .mathquill-embedded-latex 容器
    for(var i = 0; i < editAfterTxt.length; i++){
        $(editAfterTxt[i]).remove();
    }
}
```

**最终 HTML 结构：**
```html
<!-- 原始结构 -->
<span class="mathquill-embedded-latex">
    <img class="cg-math-formula" width="120px" src="data:image/png;base64,..." />
</span>

<!-- 清理后结构 -->
<img class="cg-math-formula" width="120px" src="data:image/png;base64,..." />
```

---

## 🔑 关键技术点

### 1. **为什么使用 Base64 内嵌而不是上传图床？**
✅ 优点：
- 无需额外网络请求
- 不依赖外部图床服务
- 数据完全自主可控
- 适合内网环境

❌ 缺点：
- HTML 体积增大（Base64 比原始图片大 33%）
- 导出 Word 时可能影响性能

### 2. **为什么要先 SVG → Canvas 再截图？**
```
KaTeX 渲染
    ↓
包含 SVG 的 HTML
    ↓
canvg (SVG → Canvas)  ← 必须的中间步骤
    ↓
纯 HTML + Canvas
    ↓
html2canvas (整体 → PNG)
    ↓
Base64 图片
```

**原因：** html2canvas 对 SVG 的支持不完善，直接截取会丢失样式或渲染错误。

### 3. **如何保证图片尺寸正确？**
```javascript
// 第 42 行：在渲染前记录 offsetWidth
var canvasWidth = mathFormula[j].offsetWidth;

// 第 59 行：在 <img> 中显式设置 width
width="' + domUrlArr[j].width + 'px"
```

**关键：** 使用 `offsetWidth`（CSS 渲染宽度）而非 canvas 物理宽度，确保 Word 导出时尺寸一致。

---

## 📊 与我们脚本的对比

| 特性 | 橙果官方 | um-inject.user.js（当前） |
|------|---------|---------------------------|
| **渲染引擎** | KaTeX | KaTeX |
| **SVG 处理** | canvg.js | ❌ 未处理（可能导致截图失败） |
| **图片存储** | Base64 内嵌 | 上传到 UAPIS.CN 图床 |
| **尺寸记录** | offsetWidth | offsetWidth ✅ |
| **html2canvas 配置** | `allowTaint: true, taintTest: false` | ✅ 一致 |
| **容器清理** | 清除 `.mathquill-embedded-latex` | ❌ 未清理（保留容器） |

---

## 🎯 我们脚本需要改进的地方

### ❌ 当前问题
1. **缺少 SVG → Canvas 转换步骤**
   - 导致 KaTeX 渲染的 SVG 符号截图失败
   - 解决方案：引入 canvg.js

2. **未清理 MathQuill 容器**
   - 保留了 `<span class="mathquill-embedded-latex">` 外层
   - 可能影响编辑器的后续操作

3. **上传图床而非 Base64 内嵌**
   - 增加网络依赖
   - 如需离线使用或内网环境会失败

### ✅ 改进方案
参考橙果官方逻辑：
```javascript
// 1. 加载 canvg.js
await loadCanvg();

// 2. 在 html2canvas 前先转换 SVG
svgTocanvas(container);

// 3. 保留 Base64 作为回退选项（不强制上传）
return {
    url: dataUrl, // Base64
    width: originalWidth,
    height: originalHeight
};

// 4. 清理 MathQuill 容器
function unwrapMathQuillContainers() {
    var containers = document.querySelectorAll('.mathquill-embedded-latex');
    containers.forEach(function(c){
        var img = c.querySelector('img');
        if(img) c.parentNode.replaceChild(img, c);
    });
}
```

---

## 📦 依赖库清单

### 橙果官方使用的库：
1. **KaTeX** (0.16.x) - LaTeX → HTML
2. **canvg.js** (1.x) - SVG → Canvas
3. **html2canvas** (1.4.1) - HTML → PNG
4. **jQuery** - DOM 操作

### 加载顺序（index.html）：
```html
<!-- 1. KaTeX -->
<link rel="stylesheet" href="katex/katex.min.css">
<script src="katex/katex.min.js"></script>

<!-- 2. html2canvas -->
<script src="js/html2canvas.min.js"></script>

<!-- 3. canvg（关键！） -->
<script src="js/canvg.min.js"></script>

<!-- 4. 公式转图片逻辑 -->
<script src="js/mathFormulaToImg.js"></script>
```

---

## 🚀 最佳实践建议

### 如果要完全复刻橙果逻辑：
1. ✅ 使用 KaTeX 渲染（已实现）
2. ✅ 记录 offsetWidth（已实现）
3. ❌ **添加 canvg.js 处理 SVG**（待实现）
4. ❌ **使用 Base64 内嵌而非上传**（待调整）
5. ❌ **清理 MathQuill 容器**（待实现）

### 如果保留图床上传：
1. 保留当前上传逻辑（UAPIS.CN）
2. 添加 canvg.js 处理 SVG（提高成功率）
3. Base64 作为回退方案（上传失败时）

---

## 📝 总结

**橙果官方的核心优势：**
- 完全离线可用（Base64 内嵌）
- SVG 处理完善（canvg.js）
- 容器清理干净（无冗余结构）

**我们脚本的核心优势：**
- 图片外部存储（减小 HTML 体积）
- 跨域注入能力（Tampermonkey）
- 更灵活的配置（DEBUG_MODE、图床开关）

**推荐方案：** 融合两者优势
- 添加 canvg.js（提高稳定性）
- 保留图床上传（减小体积）
- Base64 作为回退（确保可用性）
