# 🧪 测试指南

## 快速开始

### 方法 1：使用启动脚本（推荐）

在项目根目录运行：

```powershell
cd D:\VC\mathquill
.\umeditor\run-test.ps1
```

脚本会自动：
1. 检查测试文件
2. 启动本地 HTTP 服务器（端口 8000）
3. 在浏览器中打开测试页面

### 方法 2：手动启动

**启动 HTTP 服务器：**
```powershell
cd D:\VC\mathquill
python -m http.server 8000
```

**在浏览器中访问：**
```
http://localhost:8000/umeditor/test-render-validation.html
```

---

## 测试页面功能

### 📋 测试用例列表

| # | 测试场景 | LaTeX | 预期结果 |
|---|---------|-------|---------|
| 1 | 支持的公式 | `$$E = mc^2$$` | ✅ 成功渲染 |
| 2 | 不支持的命令 | `$$\mathbb{ABC}$$` | ❌ 失败，显示错误 |
| 3 | 支持的 mathbb | `$$\mathbb{N}$$` | ✅ 成功（归一化为 `\N`） |
| 4 | 混合内容 | 4 个公式（2 成功 + 2 失败） | ⚠️ 部分失败 |
| 5 | 积分公式 | `$$\int_0^\infty...$$` | ✅ 成功 |
| 6 | 集合符号 | `$$A \subset B...$$` | ✅ 成功 |

### 🎯 测试步骤

1. **打开测试页面**
   - 页面会自动加载 MathQuill 库
   - 检查控制台是否有错误

2. **逐个测试**
   - 点击每个测试用例的"测试渲染"按钮
   - 观察结果区域的显示

3. **查看验证结果**
   - ✅ **成功**：绿色背景，显示渲染后的公式
   - ❌ **失败**：红色背景，显示错误信息

4. **检查统计数据**
   - 总测试数
   - 成功渲染数
   - 渲染失败数
   - 验证执行数

5. **查看编辑器**
   - 成功的公式会插入到底部的模拟编辑器中

---

## 🔍 验证逻辑测试

### 验证函数检查点

`validateMathQuillRender()` 会检查：

1. **DOM 结构**
   - 是否存在 `.mq-root-block` 或 `.mq-math-mode`
   - 是否有数学内容节点（`var`, `.mq-non-leaf`, `.mq-scaled` 等）

2. **内容验证**
   - 是否有文本内容
   - 节点数量是否大于 0

3. **尺寸验证**
   - 渲染后的宽度和高度是否正常

### 预期测试结果

**测试 1 - 应该成功：**
```
✅ 渲染成功
验证通过
LaTeX: E = mc^2
归一化: E = mc^2
DOM节点数: > 0
```

**测试 2 - 应该失败：**
```
❌ 渲染失败
验证失败
错误: 渲染结果为空（LaTeX 可能不被支持）
LaTeX: \mathbb{ABC}
归一化: \mathbb{ABC}
DOM内容: (空或极少)
```

**测试 3 - 应该成功：**
```
✅ 渲染成功
验证通过
LaTeX: \mathbb{N}
归一化: \N  ← 注意归一化
DOM节点数: > 0
```

**测试 4 - 批量验证：**
```
批量验证完成

✅ 成功: 2 个
  • 公式 1: x^2 + y^2 = r^2
  • 公式 3: \frac{a}{b} + \sqrt{c}

❌ 失败: 2 个
  • 公式 2: \mathbb{ABC}
    错误: 渲染结果为空
  • 公式 4: \unknown{command}
    错误: 渲染结果为空
```

---

## 🐛 调试技巧

### 查看浏览器控制台

按 `F12` 打开开发者工具，查看：

1. **Console 标签**
   - MathQuill 加载日志
   - 验证执行日志
   - 错误信息

2. **Elements 标签**
   - 查看渲染后的 DOM 结构
   - 检查 `.mq-root-block` 内容

3. **Network 标签**
   - 确认 mathquill.js 和 mathquill.css 加载成功

### 常见问题

**问题 1：页面空白或样式错乱**
- 原因：资源未正确加载（CORS 限制）
- 解决：使用 HTTP 服务器而非直接打开文件

**问题 2：所有测试都显示失败**
- 原因：MathQuill 未正确初始化
- 解决：检查控制台错误，确认 jQuery 和 MathQuill 加载

**问题 3：某些命令显示为成功但实际为空**
- 原因：验证逻辑可能需要调整
- 解决：检查 DOM 内容和尺寸

---

## 📊 测试报告

完成所有测试后，记录以下信息：

### 环境信息
- 浏览器：Chrome/Firefox/Edge (版本号)
- MathQuill 版本：(从控制台查看)
- 测试时间：2025-10-19

### 测试结果

| 测试 | 预期 | 实际 | 状态 |
|-----|------|------|------|
| 测试 1 | 成功 | ? | ☐ |
| 测试 2 | 失败 | ? | ☐ |
| 测试 3 | 成功 | ? | ☐ |
| 测试 4 | 部分失败 | ? | ☐ |
| 测试 5 | 成功 | ? | ☐ |
| 测试 6 | 成功 | ? | ☐ |

### 统计数据
- 总测试数：
- 成功渲染：
- 渲染失败：
- 验证执行：

---

## 🔧 扩展测试

### 自定义测试

在测试页面的控制台中运行：

```javascript
// 测试自定义 LaTeX
renderAndValidate('你的LaTeX代码', 'result-1');

// 批量测试
var testLatexList = [
    '\\frac{1}{2}',
    '\\sqrt{x}',
    '\\mathbb{XYZ}',
    // ...
];

testLatexList.forEach(function(latex, idx) {
    console.log('测试 ' + (idx+1) + ':', latex);
    renderAndValidate(latex, 'result-1');
});
```

### 性能测试

测试大量公式的验证性能：

```javascript
console.time('批量验证');
for (var i = 0; i < 100; i++) {
    renderAndValidate('x^{' + i + '}', 'result-1');
}
console.timeEnd('批量验证');
```

---

## ✅ 测试清单

完成以下项目以确保功能正常：

- [ ] 测试页面正常加载
- [ ] MathQuill 库加载成功
- [ ] 测试 1（支持的公式）通过
- [ ] 测试 2（不支持的命令）正确显示失败
- [ ] 测试 3（mathbb单字母）通过
- [ ] 测试 4（混合内容）正确区分成功/失败
- [ ] 测试 5（积分公式）通过
- [ ] 测试 6（集合符号）通过
- [ ] 统计数据正确更新
- [ ] 成功的公式插入到编辑器
- [ ] 浏览器控制台无错误

---

## 📝 反馈

测试完成后，如果发现问题请记录：

1. **问题描述**
2. **复现步骤**
3. **预期结果 vs 实际结果**
4. **浏览器控制台错误信息**
5. **截图（如有）**

---

生成时间：2025-10-19
测试版本：um-inject.user.js (带渲染验证)
